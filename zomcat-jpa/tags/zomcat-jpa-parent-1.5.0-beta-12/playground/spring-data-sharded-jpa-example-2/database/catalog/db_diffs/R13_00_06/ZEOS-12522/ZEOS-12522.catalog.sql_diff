BEGIN;


    -- $Id$
    -- $HeadURL$

    select _v.register_patch ('ZEOS-12522.catalog');

    set role to zalando;

    insert into zcat_commons.price_fallback_appdomains (pfa_appdomain_id, pfa_fallback_sequence)
        values (201, ARRAY [1, 19, 11, 5, 25, 15, 16, 27]);

    reset role;

COMMIT;
