create type multimedia_type as (
    version                   integer,
    multimedia_type_code      text,
    name                      text,
    mime_type                 text,
    is_active                 boolean
);

