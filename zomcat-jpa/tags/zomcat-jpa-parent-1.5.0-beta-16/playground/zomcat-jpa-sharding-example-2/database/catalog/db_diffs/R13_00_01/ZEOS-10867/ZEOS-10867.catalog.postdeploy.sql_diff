BEGIN;

    -- $Id$
    -- $HeadURL$

    select _v.register_patch ('ZEOS-10867.catalog.postdeploy');

    set role to zalando;

    -- change job configuration for priceChangeExportJob
    update zcat_commons.app_config
       set ac_value = '*'
     where ac_key   = 'jobConfig.priceChangeExportJob.appInstanceKey';

    reset role;

--ROLLBACK;
COMMIT;