-- this is for sharding-config-test, because seems to be better to setup the schema on all shards at same time
CREATE SCHEMA IF NOT EXISTS zzj_data AUTHORIZATION sa;
CREATE SCHEMA IF NOT EXISTS zzj_api AUTHORIZATION sa;