create type sales_channels_release as (
    version          integer,
    lounge           boolean,
    offline_outlet   boolean,
    resale           boolean,
    emeza            boolean,
    kiomi            boolean
);
