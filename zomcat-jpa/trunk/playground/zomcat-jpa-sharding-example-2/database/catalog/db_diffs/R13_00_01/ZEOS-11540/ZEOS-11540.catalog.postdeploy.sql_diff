BEGIN;

    -- $Id:  $
    -- $HeadURL:  $

    select _v.register_patch ('ZEOS-11540.catalog.postdeploy');

    set role to zalando;

    INSERT INTO zcat_commons.app_config(ac_key, ac_value, ac_is_online_updateable)
         VALUES ('feature.eventlog.priceChangePublish', 'on', true),
                ('feature.eventlog.priceChangeArchived','on',true);

    reset role;

--ROLLBACK;
COMMIT;