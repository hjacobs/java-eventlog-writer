BEGIN;

    -- $Id: $
    -- $HeadURL: $

    select _v.register_patch ('ZEOS-9312.catalog.data');

    set role to zalando;

    alter table zcat_data.price_definition add CONSTRAINT countries_with_multiple_appdomains CHECK (pd_appdomain_id not in (21, 26, 37));

    reset role;

COMMIT;