
/**
 * This class only exist because the Maven-Jacoco-Plugin needs a directory named 'target/classes'. So if you do not have
 * Sourcefiles under 'src/main/java' there will never be a 'classes'-directory under 'target'. So this is just a stupid
 * way like all dummy-tests for UT-builds.<br/>
 * <br/>
 * I hate that way.
 *
 * @author  jbellmann
 */
public class OnlyExistForJacocoMavenPlugin { }
